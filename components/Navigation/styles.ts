import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  nav_meta: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    padding: '1rem',
    backgroundColor: '#bbb'
  }
})

export default styles